import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ustequake.com',
  appName: 'uste-quake',
  webDir: 'dist/USTeQuake',
  bundledWebRuntime: false
};

export default config;
